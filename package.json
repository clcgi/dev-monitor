{
  "name": "dev-monitor-styles",
  "private": true,
  "description": "Tailwind build for the Dioxus desktop UI. Node is a BUILD-time dependency only; the compiled CSS is baked into the Rust binary, so running the app needs Rust alone.",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
